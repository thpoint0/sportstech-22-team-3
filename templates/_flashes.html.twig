  {% for message in app.flashes('success') %}
            <div class="alert alert-success text-center col-md-6 mx-auto ">
                {{ message }}
            </div>
        {% endfor %}

          {% for message in app.flashes('warning') %}
            <div class="alert alert-warning text-center col-md-6 mx-auto ">
                {{ message }}
            </div>
        {% endfor %}



        
          {% for message in app.flashes('error') %}
            <div class="alert alert-error text-center col-md-6 mx-auto ">
                {{ message }}
            </div>
        {% endfor %}