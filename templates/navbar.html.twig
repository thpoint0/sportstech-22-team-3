

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

    <a class="navbar-brand" href="{{ path('home') }}">E-commerce</a>
    
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">

        <ul class="navbar-nav">

             <li class="nav-item active">
                <a class="nav-link" href="{{ path('home') }}">Home Page</a>
          {% if not is_granted('ROLE_ADMIN') %}
            <li class="nav-item active">




<li class="nav-item dropdown">
                    {# <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Search By</a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown"> #}
                        {# <a class="dropdown-item" href="{{ path('app_brand_index') }}">Brand</a>
                        <a class="dropdown-item" href="{{ path('app_category_index') }}">Category</a>
                        <a class="dropdown-item" href="{{ path('app_fabric_index') }}">Fabric</a>
                        <a class="dropdown-item" href="{{ path('app_product_index') }}">Product</a> #}
                    {# </div>
                </li> #}
                    {% endif %}
            {% if app.user == null %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_register') }}">sign up</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_login') }}">sign in</a>
                </li>
                
            {% endif %} 
            {% if app.user %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_profile_index') }}">Profile</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}


                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Dashboard</a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        {# <a class="dropdown-item" href="{{ path('app_user_index') }}">User Control</a>
                        <a class="dropdown-item" href="{{ path('app_admin') }}">Admin Page</a>
                        <a class="dropdown-item" href="{{ path('app_brand_index') }}">Manage Brand</a>
                        <a class="dropdown-item" href="{{ path('app_category_index') }}">Manage Category</a>
                        <a class="dropdown-item" href="{{ path('app_fabric_index') }}">Manage Fabric</a>
                        <a class="dropdown-item" href="{{ path('app_product_index') }}"> Manage Product</a> #}
                    </div>
                </li>
               
                    
                {% endif %}
                <li class="nav-item active">
                    <a class="nav-link " href="{{ path('app_logout') }}">Log out</a>
                </li>
            {% endif %}
            
        </ul>

    </div>
</nav>