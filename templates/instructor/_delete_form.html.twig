<form method="post" action="{{ path('app_instructor_delete', {'id': instructor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instructor.id) }}">
    <button class="btn">Delete</button>
</form>
